-- Simplest polymorphic test

data Nat : Type where
  Z : Nat
  S : Nat -> Nat

-- Very simple polymorphic function
const : {A : Type} -> {B : Type} -> A -> B -> A
const x y = x

main : Nat
main = const {Nat} {Nat} Z (S Z)